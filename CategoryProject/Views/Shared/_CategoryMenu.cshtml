@model List<SharedLibrary.Models.CategoryDto>
@* @if (ViewBag.CategoryId != null)
{
    <h3>Categories</h3>
    <ul>
        @foreach (var category in Model)
        {
            <li class="@(ViewBag.CategoryId == category.Id ? "active-category" : "")">
                <a asp-controller="Home" asp-action="Index" asp-route-categoryId="@category.Id">
                    @category.Name
                </a>
            </li>
        }
    </ul>
}
else
{
    <h3>Categories</h3>
    <ul>
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="Home" asp-action="Index" asp-route-categoryId="@category.Id">
                    @category.Name
                </a>
            </li>
        }
    </ul>
}
 *@